[package]
name = "openpbr"
version = "0.1.0"
edition = "2024"

[features]
rand = ["dep:rand"]

[dependencies]
enum-map = "2.7.3"
glam = "0.32.1"
rand = { version = "0.9", optional = true }

[dev-dependencies]
openpbr-oracle = { path = "../openpbr-oracle" }
